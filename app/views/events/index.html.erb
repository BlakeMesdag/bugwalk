<div class="row">
  <div class="span9">
    <% if @this_weeks_events.any? %>
      <h2>This Weeks Event</h2>
      <%= render @this_weeks_events %>
    <% end %>
    
    <% if @events.any? %>
      <h2>Upcoming Events</h2>
      <%= render @events %>
    <% end %>
  </div>

  <div class="span3">
    <h4>Bug /bəg/</h4>
    <p>An error in a computer program or system</p>
    <h1>+</h1>
    <h4>Walk /wôk/</h4>
    <p>Move at a regular and fairly slow pace by lifting and setting down each foot in turn, never having both feet off the ground at once</p>
    <h1>=</h1>
    <h4>Bug Walk /bəg wôk/</h4>
    <p>Moving at a regular and fairly slow pace through a computer program or system in order to solve a an error</p>
  </div>
</div>